import { ModalHostDirective } from './modal-host.directive';

describe('ModalHostDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new ModalHostDirective();
  //   expect(directive).toBeTruthy();
  // });
});
